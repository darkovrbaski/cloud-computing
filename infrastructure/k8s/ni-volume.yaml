apiVersion: v1
kind: PersistentVolume
metadata:
  name: ni-postgres-pv
  labels:
    app: ni-lib
spec:
  capacity:
    storage: 256Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: "/mnt/ni-postgres-pv"
  storageClassName: "standard"
  volumeMode: Filesystem
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ni-postgres-pv-claim
  labels:
    app: ni-lib
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  storageClassName: "standard"
  volumeMode: Filesystem
