apiVersion: v1
kind: PersistentVolume
metadata:
  name: ns-postgres-pv
  labels:
    app: ns-lib
spec:
  capacity:
    storage: 256Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: "/mnt/ns-postgres-pv"
  storageClassName: "standard"
  volumeMode: Filesystem
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ns-postgres-pv-claim
  labels:
    app: ns-lib
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
  storageClassName: "standard"
  volumeMode: Filesystem
